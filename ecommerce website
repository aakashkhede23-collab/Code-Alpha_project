<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>BlueKart | E-Commerce Website</title>

<style>
*{
  margin:0;
  padding:0;
  box-sizing:border-box;
  font-family:"Segoe UI",Arial,sans-serif;
}

body{
  background:#f1f5f9;
  color:#1f2937;
}

/* ===== HEADER ===== */
header{
  background:linear-gradient(90deg,#2563eb,#1d4ed8);
  padding:14px 30px;
  display:flex;
  align-items:center;
  justify-content:space-between;
  color:white;
}

.logo{
  font-size:26px;
  font-weight:bold;
}

.search{
  width:40%;
}
.search input{
  width:100%;
  padding:9px;
  border:none;
  border-radius:4px;
  outline:none;
}

.header-right{
  display:flex;
  align-items:center;
  gap:20px;
}

.cart{
  cursor:pointer;
  font-weight:bold;
}

.login-btn{
  background:white;
  color:#2563eb;
  padding:6px 12px;
  border-radius:4px;
  cursor:pointer;
  font-weight:bold;
}

/* ===== HERO ===== */
.hero{
  background:linear-gradient(135deg,#dbeafe,#eff6ff);
  padding:45px 30px;
  display:flex;
  align-items:center;
  justify-content:space-between;
}

.hero-text h1{
  font-size:38px;
  color:#1e3a8a;
}
.hero-text p{
  margin-top:10px;
  color:#475569;
}
.hero-offer{
  font-size:42px;
  color:#2563eb;
  font-weight:bold;
}

/* ===== PRODUCTS ===== */
.container{
  padding:35px;
}

.title{
  font-size:24px;
  margin-bottom:20px;
  color:#1e40af;
}

.products{
  display:grid;
  grid-template-columns:repeat(auto-fill,minmax(220px,1fr));
  gap:22px;
}

.card{
  background:white;
  border-radius:14px;
  padding:15px;
  box-shadow:0 12px 30px rgba(0,0,0,0.12);
  transition:0.35s;
}
.card:hover{
  transform:translateY(-8px);
  box-shadow:0 18px 40px rgba(37,99,235,0.25);
}

.image{
  height:180px;
  background:#eff6ff;
  border:1px solid #bfdbfe;
  border-radius:12px;
  display:flex;
  align-items:center;
  justify-content:center;
  font-size:42px;
}

.card h3{
  font-size:15px;
  margin:12px 0 6px;
}

.price{
  color:#16a34a;
  font-weight:bold;
  margin-bottom:10px;
}

button{
  width:100%;
  padding:9px;
  background:linear-gradient(90deg,#2563eb,#1d4ed8);
  border:none;
  border-radius:6px;
  color:white;
  font-weight:bold;
  cursor:pointer;
}

/* ===== LOGIN MODAL ===== */
.modal{
  position:fixed;
  top:0; left:0;
  width:100%; height:100%;
  background:rgba(0,0,0,0.5);
  display:none;
  align-items:center;
  justify-content:center;
}

.modal-box{
  background:white;
  padding:25px;
  width:300px;
  border-radius:10px;
}

.modal-box h2{
  text-align:center;
  margin-bottom:15px;
  color:#2563eb;
}

.modal-box input{
  width:100%;
  padding:8px;
  margin-bottom:10px;
}

.modal-box button{
  margin-top:5px;
}

.modal-box p{
  text-align:center;
  margin-top:10px;
  cursor:pointer;
  color:#2563eb;
}

/* ===== FOOTER ===== */
footer{
  background:#1e3a8a;
  text-align:center;
  padding:16px;
  margin-top:45px;
  color:#e0f2fe;
}
</style>
</head>

<body>

<header>
  <div class="logo">BlueKart</div>
  <div class="search">
    <input type="text" placeholder="Search products, brands & more">
  </div>
  <div class="header-right">
    <div class="cart">🛒 Cart (<span id="cartCount">0</span>)</div>
    <div class="login-btn" onclick="openLogin()">Login</div>
  </div>
</header>

<section class="hero">
  <div class="hero-text">
    <h1>Shop Smart, Live Better</h1>
    <p>Electronics • Fashion • Lifestyle</p>
  </div>
  <div class="hero-offer">50% OFF</div>
</section>

<div class="container">
  <div class="title">Top Deals For You</div>

  <div class="products">
    <div class="card"><div class="image">📱</div><h3>Smartphone</h3><div class="price">₹14,999</div><button onclick="addToCart()">Add to Cart</button></div>
    <div class="card"><div class="image">💻</div><h3>Laptop</h3><div class="price">₹54,999</div><button onclick="addToCart()">Add to Cart</button></div>
    <div class="card"><div class="image">🎧</div><h3>Headphones</h3><div class="price">₹2,999</div><button onclick="addToCart()">Add to Cart</button></div>
    <div class="card"><div class="image">👟</div><h3>Shoes</h3><div class="price">₹1,999</div><button onclick="addToCart()">Add to Cart</button></div>
  </div>
</div>

<footer>
  © 2025 BlueKart — Stylish Online Shopping Website
</footer>

<!-- LOGIN / SIGNUP MODAL -->
<div class="modal" id="authModal">
  <div class="modal-box">
    <h2 id="authTitle">Login</h2>
    <input type="email" id="email" placeholder="Email">
    <input type="password" id="password" placeholder="Password">
    <button onclick="submitAuth()">Submit</button>
    <p onclick="toggleAuth()">Switch to Signup</p>
  </div>
</div>

<script>
let cart=0;
function addToCart(){
  if(!localStorage.getItem("user")){
    alert("Please login first");
    return;
  }
  cart++;
  document.getElementById("cartCount").innerText=cart;
}

/* LOGIN SYSTEM */
let isLogin=true;

function openLogin(){
  document.getElementById("authModal").style.display="flex";
}

function toggleAuth(){
  isLogin=!isLogin;
  document.getElementById("authTitle").innerText=isLogin?"Login":"Signup";
}

function submitAuth(){
  let email=document.getElementById("email").value;
  let password=document.getElementById("password").value;

  if(isLogin){
    let user=JSON.parse(localStorage.getItem("user"));
    if(user && user.email===email && user.password===password){
      alert("Login successful");
      document.getElementById("authModal").style.display="none";
    }else{
      alert("Invalid credentials");
    }
  }else{
    localStorage.setItem("user",JSON.stringify({email,password}));
    alert("Signup successful");
    isLogin=true;
    document.getElementById("authTitle").innerText="Login";
  }
}
</script>

</body>
</html>
